
cluster:
  provider: ansible
  roles: server-admin
  config: ansible.cfg
  group_vars: variables
  playbooks: playbooks/cluster.yml
  local_home_dir: /var/local/cenv

client:
  provider: ansible
  roles: server-admin
  config: ansible.cfg
  group_vars: variables
  playbooks: playbooks/client.yml
  local_home_dir: /var/local/cenv
