env:
  HELM_TLS_VERIFY: false
  HELM_TLS_ENABLE: true
scripts: scripts/install.sh
requirements:
  - kubespray/requirements.txt
  - requirements.txt
profiles: profiles
tasks: tasks
lib: false
modules:
  cluster:
    provider: git
    remote: https://github.com/venturiscm/cenv-cluster.git
    reference: master
